#include<stdio.h>
int main(){
char A[100];
int i,count=0;
printf("Enter the Characters to count");
scanf("%s",A[i]);
for(i=0;A[i]!='\0';i++){
count++;
}
printf("the character count is %d",count);
return 0;
}
